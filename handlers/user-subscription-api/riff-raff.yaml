# MANAGED FILE: to push changes see buildcheck/README.md - template: buildcheck/data/templates/handler/riff-raff.yaml.ts
stacks:
  - support
regions:
  - eu-west-1
allowedStages:
  - CODE
  - PROD
deployments:
  user-subscription-api-cloudformation:
    type: cloud-formation
    app: user-subscription-api
    parameters:
      templateStagePaths:
        CODE: user-subscription-api-CODE.template.json
        PROD: user-subscription-api-PROD.template.json
  user-subscription-api:
    type: aws-lambda
    parameters:
      fileName: user-subscription-api.zip
      bucketSsmLookup: true
      prefixStack: false
      functionNames:
        - user-subscription-api-
    dependencies:
      - user-subscription-api-cloudformation
