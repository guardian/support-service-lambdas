{
  "name": "discount-api",
  "scripts": {
    "test": "jest --group=-integration",
    "it-test": "jest --group=integration",
    "type-check": "tsc --noEmit",
    "build": "esbuild --bundle --platform=node --target=node18 --outdir=target/ src/index.ts",
    "lint": "eslint src/**/*.ts test/**/*.ts",
    "package": "pnpm type-check && pnpm lint && pnpm check-formatting && pnpm test && pnpm build && cd target && zip -qr discount-api.zip ./*.js",
    "check-formatting": "prettier --check **.ts",
    "fix-formatting": "prettier --write **.ts"
  },
  "NOTICE": "To edit - 1. update buildcheck/src/data/build.ts - 2. run `pnpm update-build` - template: buildcheck/src/data/handlerTemplate/lambda/package.json.ts",
  "dependencies": {
    "dayjs": "^1.11.13",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147"
  }
}