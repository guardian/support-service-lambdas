// import { allTests } from '../fixtures/testDataForTimelineParser';
// import { unflattenOrder } from '@modules/subscription-sync/src/buildOrder/unflattenOrder';
//
// test.each(
// 	Object.entries(allTests).filter(
// 		([, value]) => value(false).flattened !== undefined,
// 	),
// )(
// 	'unflattenOrder expands relative events as expected for %s',
// 	(_, testData) => {
// 		const actual = unflattenOrder(testData(false).flattened!);
// 		expect(actual).toStrictEqual(testData(false).);
// 	},
// );
