name: CORE4 label enforcement
permissions:
  contents: read
on:
  pull_request:
    types: [opened, labeled, unlabeled, synchronize, reopened, edited, ready_for_review]
jobs:
  require-label:
    uses: guardian/.github/.github/workflows/require-label.yaml@v3.0.0
